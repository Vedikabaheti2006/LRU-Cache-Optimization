# LRU-Cache-Optimization
High-performance LRU Cache implementation in C++.

## 🚀 Features
* Fast $O(1)$ access and update times.
* Template-based for flexible key-value types.
* Optimized memory management.

## 🛠️ Usage
```cpp
LRUCache<int, string> cache(2);
cache.put(1, "data1");
cout << cache.get(1); // Returns "data1"

```
